# Development Guide

## 🚨 Почему возникают ошибки и как их избежать

### Основные причины проблем:

1. **Дубликаты кода**
   - **Причина**: Случайное копирование/вставка при редактировании
   - **Решение**: Всегда проверяй файл после редактирования, используй `scripts/check-duplicates.sh`

2. **Несуществующие импорты**
   - **Причина**: Копирование кода без проверки, что функция существует
   - **Решение**: TypeScript должен ловить это, но если нет - запусти `npm run type-check`

3. **Ошибки билда**
   - **Причина**: Код не проверяется перед коммитом/деплоем
   - **Решение**: Используй pre-commit hooks и CI/CD

4. **Проблемы с деплоем**
   - **Причина**: Деплой без проверки билда локально
   - **Решение**: Всегда запускай `npm run predeploy` перед деплоем

## ✅ Workflow для безопасной разработки

### Перед каждым коммитом:
```bash
npm run check  # lint + type-check
```

### Перед деплоем:
```bash
npm run predeploy  # check + build
```

### Если что-то сломалось:
1. Проверь ошибки билда: `npm run build`
2. Проверь типы: `npm run type-check`
3. Проверь линтер: `npm run lint`
4. Проверь дубликаты: `bash scripts/check-duplicates.sh`

## 🔧 Настройка pre-commit hooks

```bash
# Установи husky (если еще не установлен)
npm install --save-dev husky

# Инициализируй husky
npx husky init

# Хуки уже созданы в .husky/pre-commit
```

## 📋 Checklist перед деплоем

- [ ] `npm run check` проходит без ошибок
- [ ] `npm run build` проходит успешно
- [ ] Проверены все измененные файлы на дубликаты
- [ ] Все импорты существуют (TypeScript не ругается)
- [ ] Тесты (если есть) проходят

## 🐛 Частые ошибки и их решения

### "Cannot read properties of undefined (reading 'length')"
- **Причина**: Обращение к свойству undefined объекта
- **Решение**: Добавь проверки `?.length ?? 0` или `array?.length`

### "Module has no exported member"
- **Причина**: Импорт несуществующей функции
- **Решение**: Проверь экспорты в исходном файле, используй `npm run type-check`

### "Build failed"
- **Причина**: Ошибки TypeScript или синтаксиса
- **Решение**: Запусти `npm run build` локально и исправь все ошибки

### "504 Gateway Timeout" в проде
- **Причина**: Приложение не запускается (ошибка билда на сервере)
- **Решение**: Проверь логи EB, убедись что билд проходит локально

## 🔄 CI/CD Pipeline

GitHub Actions автоматически проверяет:
- Линтинг
- Type checking
- Билд

Если что-то не проходит - деплой не должен происходить.

## 💡 Best Practices

1. **Всегда проверяй билд локально** перед деплоем
2. **Используй TypeScript strict mode** (уже включен)
3. **Не коммить код, который не билдится**
4. **Проверяй импорты** перед использованием
5. **Используй pre-commit hooks** для автоматической проверки

